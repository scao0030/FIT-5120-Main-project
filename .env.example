SAFE_BROWSING_API_KEY=
ABUSECH_AUTH_KEY=
VITE_ENTRY_PASSWORD=fit5120
PORT=5174
HOST=0.0.0.0

# If the frontend is hosted on a different domain, set CORS_ORIGIN to that origin.
# CORS_ORIGIN=http://localhost:5173
# CORS_ORIGIN=https://your-frontend.example.com
# CORS_ORIGIN=*

# For production builds, point the frontend at the deployed API origin.
# VITE_API_BASE_URL=https://api.your-domain.example.com
